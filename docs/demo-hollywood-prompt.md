# Hollywood Demo Script Prompt

Use this prompt to regenerate the sanitized demo screenplay if needed. Do not include local paths, real character names, private role presets, LAN IPs, generated audio paths, API keys, or provider-specific credentials in the committed result.

```text
Write a compact Hollywood screenplay demo for a TTS production workbench.

Requirements:
- Use neutral fictional names only.
- Include 3 short scenes with scene headings in screenplay style.
- Include narrator, protagonist, antagonist, team/ensemble line, SFX, MUSIC, and ON SCREEN text.
- Include emotional parentheticals and both short and long dialogue lines.
- Keep dialogue suitable for line-level TTS generation.
- Do not reference real people, local files, private services, LAN IPs, or proprietary character presets.
- Output should be safe as a static repository fixture.
```
